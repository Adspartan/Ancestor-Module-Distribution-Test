<Window x:Class="ancestor_mod_distribution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ancestor_mod_distribution"
        mc:Ignorable="d"
        Title="Ancestor Module Distribution Test" Height="600" Width="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="50"/>
    </Grid.RowDefinitions>
    <DataGrid x:Name="result_datagrid" AutoGenerateColumns="False" FontSize="15">
      <DataGrid.Resources>
        <Style TargetType="{x:Type DataGridColumnHeader}">
          <Setter Property="Height" Value="30"/>
          <Setter Property="HorizontalAlignment" Value="Stretch"/>
          <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="{x:Type DataGridCell}">
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="DataGridCell">
                <Grid>
                  <ContentPresenter HorizontalAlignment="Center"/>
                </Grid>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </DataGrid.Resources>
      <DataGrid.Columns>
        <DataGridTextColumn Header="Descendant" Width="150" Binding="{Binding descendant}"/>
        <DataGridTextColumn Header="Count" Width="75" Binding="{Binding count}"/>
      </DataGrid.Columns>
    </DataGrid>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Label Content="Module Count:" VerticalAlignment="Center"/>
      <TextBox x:Name="module_count_tb" Text="100" PreviewTextInput="module_count_tb_PreviewTextInput" Margin="5" Grid.Column="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
      <Button x:Name="run_btn" Content="Run" Margin="5" Click="run_btn_Click" Grid.Column="2"/>
    </Grid>
  </Grid>
</Window>
